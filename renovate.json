{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "GitHub Actions version bumps touch .github/workflows/**, which our own AI sweep deliberately refuses to merge (GITHUB_TOKEN can't write workflow files without `workflow` scope). Renovate's own app install already has that permission, and it still waits for our required checks (checks, workflows from pr-checks.yml) to pass before merging -- so this closes the gap without giving the AI bot any extra scope.",
      "matchManagers": ["github-actions"],
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash"
    }
  ]
}
